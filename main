<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>PussycatTech</title>
  <style>
    body {
      margin: 0;
      background: #0f0f0f;
      color: #fff;
      font-family: Arial;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      text-align: center;
    }
    img { width: 160px; }
  </style>
</head>
<body>
  <div>
    <img src="logo.png" />
    <h1 id="title"></h1>
    <p id="desc"></p>
  </div>

  <script>
    fetch('data.json')
      .then(res => res.json())
      .then(data => {
        document.getElementById('title').innerText = data.name;
        document.getElementById('desc').innerText = data.description;
      });
  </script>
</body>
</html>
